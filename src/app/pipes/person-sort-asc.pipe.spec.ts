import { PersonSortAscPipe } from './person-sort-asc.pipe';

describe('PersonSortAscPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonSortAscPipe();
    expect(pipe).toBeTruthy();
  });
});
