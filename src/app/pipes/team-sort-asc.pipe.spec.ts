import { TeamSortAscPipe } from './team-sort-asc.pipe';

describe('TeamSortAscPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamSortAscPipe();
    expect(pipe).toBeTruthy();
  });
});
