export class Person {
    firstName: string;
    lastName: string;
    position: string;
    teamName: string;

  constructor(firstName: string, lastName: string, position: string, teamName: string) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.position = position;
    this.teamName = teamName;
  }
}
